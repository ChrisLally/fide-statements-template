---
title: "Statement Builder"
description: Generate statement Fide IDs from subject, predicate, and object Fide IDs.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="info" title="API + SDK Implementation">
This tool implements [API](/docs/api/v1/fide-id/) and [SDK](/docs/sdks/js/statement/#createstatement).
</Callout>

<StatementBuilder />

Provide full Fide IDs for:

- Subject
- Predicate (must be `0x65` or `0xe5` typed)
- Object

The tool calls `POST /v1/fide-id/statement` and returns the canonical statement Fide ID.
