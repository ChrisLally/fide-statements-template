---
title: "expandPredicateIdentifier"
description: "Expand shorthand predicate identifiers to full canonical URLs.
`calculateFideId` stays dumb; this provides a convenience layer for predicates.

Accepts shorthand (e.g. `schema:name`, `owl:sameAs`) and returns the full IRI.
Pass-through for strings that already look like full URLs (contain `://`)."
full: true
---

<SDKFunctionPage data={{"name":"expandPredicateIdentifier","description":"Expand shorthand predicate identifiers to full canonical URLs.\n`calculateFideId` stays dumb; this provides a convenience layer for predicates.\n\nAccepts shorthand (e.g. `schema:name`, `owl:sameAs`) and returns the full IRI.\nPass-through for strings that already look like full URLs (contain `://`).","section":"Schema","signatures":[{"description":"Expand shorthand predicate identifiers to full canonical URLs.\n`calculateFideId` stays dumb; this provides a convenience layer for predicates.\n\nAccepts shorthand (e.g. `schema:name`, `owl:sameAs`) and returns the full IRI.\nPass-through for strings that already look like full URLs (contain `://`).","parameters":[{"name":"predicate","type":"string","optional":false,"description":"Shorthand (e.g. `schema:name`) or full URL"}],"returnType":"string","returnDescription":"Full canonical URL (e.g. `https://schema.org/name`)","exampleCode":"expandPredicateIdentifier('schema:name')     // 'https://schema.org/name'\nexpandPredicateIdentifier('owl:sameAs')      // 'https://www.w3.org/2002/07/owl#sameAs'\nexpandPredicateIdentifier('https://schema.org/name')  // unchanged","referencedTypes":[]}],"constants":[],"referencedTypes":[]}} />
