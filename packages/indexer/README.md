# @fide.work/indexer

Indexer domain package for external-source ingestion adapters.

Current scope:

- GitHub statements-webhook payload validation
- statement-batch path validation
- normalized source batch refs for downstream graph ingest
