{
  "name": "@fide.work/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit",
    "sb": "mkdir -p src/types && pnpm exec dotenv -e ../.env -- npx supabase gen types typescript --project-id qssddvtvjopazmhocivh --schema public > src/types/supabase.ts"
  },
  "dependencies": {
    "@fide.work/fcp": "workspace:*",
    "drizzle-orm": "^0.44.7",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "@types/node": "^20.11.17",
    "dotenv-cli": "^10.0.0",
    "typescript": "^5.8.3"
  }
}
